## Visual assets

Recommended visuals to add (make sure they contain **no credentials**, **no project IDs**, and **no private URLs**):

- A terminal run showing a full workflow execution with stage headings
- A snippet of the generated Markdown report (redacted if needed)
- A workflow diagram screenshot (Mermaid rendered view)

